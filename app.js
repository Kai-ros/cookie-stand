'use strict';

// object literals for each shop location
// 
// need to store:
// min hourly customers
// max hourly customers
// calculate avg cookies per customer per hour
// math.random method applied to ObjLit to derive customers per hr
// results for each location in a separate array, possibly as a property of that given location Obj
// 
// Display the values of each array as <ul> in browser

// hours of operation: 6a-8p

// add/remove locations from the daily report

var stores = {
  storeLocation : '',
  minimumCustomersPerHour : 0,
  maximumCustomersPerHour : 0,
  averageCustomersPerHour : 0,
  


};
